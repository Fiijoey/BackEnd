<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <nav><%= nav %></nav>
  </header>
  <main>
    <h1><%= vehicleData.make %> <%= vehicleData.model %></h1>
    <img src="<%= vehicleData.image %>" alt="<%= vehicleData.make %> <%= vehicleData.model %>" class="vehicle-image">
    <p><strong>Year:</strong> <%= vehicleData.year %></p>
    <p><strong>Price:</strong> <%= vehicleData.price %></p>
    <p><strong>Mileage:</strong> <%= vehicleData.mileage %> miles</p>
    <p><strong>Description:</strong> <%= vehicleData.description %></p>
    <p><strong>Color:</strong> <%= vehicleData.color %></p>
  </main>
  <footer>
    <p>&copy; 2025 Your Company Name</p>
  </footer>
</body>
</html>
